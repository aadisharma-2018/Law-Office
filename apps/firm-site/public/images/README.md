# Images (`public/images/`)

Place **optimized** firm-owned assets here (PNG/JPEG/WebP/SVG). Reference them from pages with `next/image` and **required `alt`** text.

- Prefer **width/height** (or explicit `sizes`) to limit **CLS** (SC-001).
